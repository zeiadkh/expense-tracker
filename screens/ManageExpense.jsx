import {Text, StyleSheet} from "react-native"
export default function ManageExpense(){
    return <Text style={styles.temp}>ManageExpense</Text>
}
const styles = StyleSheet.create({
    temp: {
        marginTop: 400
    }
})